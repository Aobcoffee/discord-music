{
  "name": "Discord Music Bot",
  "description": "Discord music bot with YouTube and Spotify integration including FFmpeg support",
  "buildpacks": [
    {
      "url": "https://github.com/jonathanong/heroku-buildpack-ffmpeg-latest.git"
    },
    {
      "url": "heroku/python"
    }
  ],
  "env": {
    "DISCORD_BOT_TOKEN": {
      "description": "Discord bot token from Discord Developer Portal",
      "required": true
    },
    "SPOTIFY_CLIENT_ID": {
      "description": "Spotify app client ID (optional for Spotify features)",
      "required": false
    },
    "SPOTIFY_CLIENT_SECRET": {
      "description": "Spotify app client secret (optional for Spotify features)",
      "required": false
    },
    "DISCORD_COMMAND_PREFIX": {
      "description": "Bot command prefix",
      "value": "/",
      "required": false
    }
  },
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "basic"
    }
  },
  "stack": "heroku-22"
}